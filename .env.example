# Server (PostgreSQL runs via Docker — see docker-compose.yml)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/echobloom?schema=public"
CLIENT_ORIGIN="http://localhost:5173"
PORT=3001
UPLOAD_DIR="./uploads"
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="noreply@echobloom.org"
NOTIFY_EMAIL="EchoBloom@echo.com"